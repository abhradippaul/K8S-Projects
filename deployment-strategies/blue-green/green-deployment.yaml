apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: online-shop-green
  name: online-shop-green-deploy
  namespace: bluegreen-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: online-shop-green
  strategy:
    type: RollingUpdate
    rollingUpdate: 
      maxSurge: 1 #Extra pod
      maxUnavailable: 0 #Unavailable pod
  template:
    metadata:
      labels:
        app: online-shop-green
    spec:
      containers:
        - image: amitabhdevops/online_shop_without_footer
          name: online-shop-container-green
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: 25m
              memory: 50Mi
            requests:
              cpu: 10m
              memory: 20Mi

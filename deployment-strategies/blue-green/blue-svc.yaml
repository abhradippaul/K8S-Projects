apiVersion: v1
kind: Service
metadata:
  labels:
    app: online-shop-blue
  name: online-shop-blue-svc
  namespace: bluegreen-ns
spec:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: online-shop-blue
